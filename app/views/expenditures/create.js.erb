$("#modal-window").modal("hide");
  $('#expenditures_table > tbody')
    .prepend($('<%= j render @expenditure %>')
      .hide()
      .fadeIn('normal')
    );

$('#expenditure_<%= @expenditure.id %>')
  .animate({backgroundColor: "#afedc3"}, 200)
  .animate({backgroundColor: "#fff"}, 400)
